// ROLE = SUPER_ADMIN
// STATUS = ACTIVE

// setup.config.js
const SetupConfig = {
  // hard-coded system role
  SUPER_ADMIN_ROLE: 'SUPER_ADMIN',

  // default status
  DEFAULT_STATUS: 'ACTIVE'
}

module.exports = SetupConfig
